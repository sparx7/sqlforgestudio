<Window x:Class="SqlForgeWpf.Windows.ResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Query Analysis &amp; AI Export" Height="700" Width="1100"
        Background="{DynamicResource BgWindow}" FontFamily="Segoe UI"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,15">
            <Button Content="📄 EXPORT HTML" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" Click="BtnSaveHtml_Click"/>
            <Button Content="🖨️ SAVE PDF / PRINT" Style="{StaticResource SecondaryButton}" Click="BtnSavePdf_Click"/>
        </StackPanel>

        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextMuted}"/>
                    <Setter Property="Padding" Value="15,10"/>
                    <Setter Property="BorderThickness" Value="0,0,0,3"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource AccentPrimary}"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource AccentPrimary}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TabControl.Resources>

            <TabItem Header="DATA VIEW">
                <Border Background="{DynamicResource BgPanel}" CornerRadius="8" Padding="10" Margin="0,15,0,0">
                    <WebBrowser x:Name="HtmlBrowser"/>
                </Border>
            </TabItem>

            <TabItem Header="✨ AI INSIGHTS">
                <Border Background="{DynamicResource BgPanel}" CornerRadius="8" Padding="20" Margin="0,15,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Button x:Name="BtnGenerateSummary" Content="GENERATE AI SUMMARY" Margin="0,0,0,10" Click="BtnGenerateSummary_Click"/>
                        <ProgressBar x:Name="SummaryLoadingBar" Grid.Row="1" Height="3" Margin="0,0,0,15" Background="Transparent" Foreground="{DynamicResource AccentPrimary}" BorderThickness="0" IsIndeterminate="True" Visibility="Collapsed"/>

                        <WebBrowser x:Name="SummaryBrowser" Grid.Row="2"/>
                    </Grid>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</Window>